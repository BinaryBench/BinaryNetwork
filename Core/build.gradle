configurations {
    includeInJar {
        transitive = false
    }
}

dependencies {
    includeInJar "org.apache.commons:commons-dbcp2:2.1.1"

    configurations.compile.extendsFrom(configurations.includeInJar)
}

jar {
    from configurations.includeInJar.collect { zipTree it }
}
